import React from "react"
import AuthForm from "../_component/AuthForm"

export const metadata: any = {
  title: "Wind AI | Sign up",
}
const page = () => {
  return <AuthForm formType="signup" />
}

export default page
