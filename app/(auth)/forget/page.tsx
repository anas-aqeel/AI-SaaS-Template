import React from "react"
import AuthForm from "../_component/AuthForm"

export const metadata: any = {
  title: "Wind AI | Forgot password",
}
const page = () => {
  return <AuthForm formType="forget" />
}

export default page
